import 'package:flutter/material.dart';
import '../data/mock_data.dart';

class Categoria{
    final String id;
    final String titulo;
    final Color cores;
    Categoria({@required this.id, @required this.titulo, @required this.cores});
}